# Production build — copy to `frontend/.env.production` before `bun run build` / `bun run tauri build`.
# Must point at the backend `/api` base URL as reachable from the app (same PC: localhost/127.0.0.1).

VITE_API_URL=http://127.0.0.1:5001/api
